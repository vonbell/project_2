<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Finsta</title>
</head>
<body>
    <%- include('./partials/header') %>

    <main id="whole-profile">
        <header id="profile-header">
            <div id="avi-section">
                <img src="<%= post.avi %>" alt="<%= post.name %>" height="150" width="150" id="profile-avi">
            </div>
            <div id="username-bio">
                <h3><%= post.name %></h3>
                <h4><%= post.bio %></h4>
            </div>
        </header>

        <div id="post">
            <p><img src="https://www.seekpng.com/png/detail/294-2945990_there-is-a-square-grid-icon.png" alt="post image" height="12" width="12"></p>
            <p>Posts</p>
        </div>

        <div class="profile-gallery">
            <img src="<%= post.img %>" alt="<%= post.name %>" height="293" width="293">
        </div>

    </main>
</body>
</html>